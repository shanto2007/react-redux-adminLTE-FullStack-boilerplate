import WebFont from 'webfontloader'

/**
 * Procesed by webpack use this as a starting point
 * or create multiple small bundle for each page, your know what to do.
 */

WebFont.load({
  google: {
    families: ['Lato'],
  },
})
