const { testenv } = global
var app = require(testenv.app)
var Day = require(testenv.serverdir + 'models/team.model')
var chai = require('chai')
var chaiHttp = require('chai-http')
var expect = require('expect')

describe('Team - Model', () => {

})
